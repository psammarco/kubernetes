apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: safari-pvc
  namespace: project-tiger
spec:
  volumeName: safari-pv
  storageClassName: ""
  resources:
          requests:
                   storage:
                           2Gi
  accessModes: 
        - ReadWriteOnce
