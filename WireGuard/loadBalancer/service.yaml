apiVersion: v1
kind: Service
metadata:
  name: nginx-udp
  namespace: wireguard
spec:
  type: NodePort
  selector:
    app: nginx-udp
  ports:
  - port: 51820
    targetPort: 51820
    nodePort: 31820
    protocol: UDP
